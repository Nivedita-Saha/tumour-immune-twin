name: twin
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - numpy
      - scipy
      - matplotlib
      - pandas
      - torch
      - torchdiffeq
      - gymnasium
      - stable-baselines3[extra]
